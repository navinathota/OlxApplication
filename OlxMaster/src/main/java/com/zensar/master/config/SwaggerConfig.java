package com.zensar.master.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
	/*@Bean
	public Docket getCustomizedDocket() {
		return new Docket(DocumentationType.SWAGGER_2)
				.select()
				.apis(RequestHandlerSelectors.basePackage("com.zensar"))
				.build()
				.apiInfo(getApiInfo());
	}

	private ApiInfo getApiInfo() {
		return new ApiInfo("OLX master Management", "This application manages master data", "1.0.0", "http://www.zensar.com", "Naveena","apache", "https://apache.org/licenses/");
	}*/

}
